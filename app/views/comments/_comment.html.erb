<div class="card my-2 ">
  <div class="card-body fix-py-8">
    <div class="row">
      <div class="col-md-2 text-center">
        <%= image_tag comment.user.load_avatar, class: "img  img-fluid rounded-circle img-comment" %>
        <p class="text-secondary text-center"><%= comment.show_time_create %></p>
      </div>
      <div class="col-md-10">
        <p>
          <%= link_to user_path(comment.user) do %>
            <strong><%= comment.user.name %></strong>
          <% end %>
          </a>
        </p>
        <div class="clearfix"></div>
          <p class="comment-content"><%= comment.content %></p>
          <div class="float-right">
            <% if current_user && current_user.has_role?(:admin) %>
              <%= link_to product_comment_path(@product.id, comment.id), method: :delete, remote: true, 
              data: { confirm: t(".you_are_sure") } do %>
                <icon class="fa fa-trash fa-1x"></icon>
              <% end %>
            <% end %>
          </div>
          <div class="clearfix"></div>
      </div>
    </div>
  </div>
</div>
